// Generated by CoffeeScript 1.7.1
var add_group, analyze_selected_experiments, clear_groups, toggle_duplicates;

add_group = function() {
  var box, checked_boxes, myurl, paramdiv, suffix, _i, _j, _len, _len1, _ref, _ref1;
  checked_boxes = new Object();
  _ref = document.getElementById('form').children;
  for (_i = 0, _len = _ref.length; _i < _len; _i++) {
    paramdiv = _ref[_i];
    checked_boxes[paramdiv.id] = [];
    _ref1 = paramdiv.children;
    for (_j = 0, _len1 = _ref1.length; _j < _len1; _j++) {
      box = _ref1[_j];
      if (box.checked) {
        checked_boxes[paramdiv.id].push(box.name);
      }
    }
  }
  suffix = jQuery.param(checked_boxes);
  suffix = "?".concat(suffix);
  myurl = "/add_group".concat(suffix);
  $.ajax({
    url: myurl,
    success: function(data) {
      var d;
      d = $('#groups-div');
      d[0].className = "visible_div";
      d.html(data);
      $(".table").tablesorter;
    }
  });
};

clear_groups = function() {
  return $.ajax({
    url: '/clear_groups',
    success: function(data) {
      $('#groups-div').html("");
      $('#results-div').html("");
      $('#groups-div')[0].className = "invisible_div";
      $(".table").tablesorter;
    }
  });
};

toggle_duplicates = function() {
  return $.ajax({
    url: '/toggle_duplicates',
    success: function(data) {
      $('#groups-div').html(data);
      $('#groups-div')[0].className = "visible_div";
      return $(".table").tablesorter;
    }
  });
};

analyze_selected_experiments = function() {
  $.ajax({
    url: "/analyse",
    success: function(data) {
      $("#results-div").html(data);
      $(".table").tablesorter;
    }
  });
};

//# sourceMappingURL=index.map
